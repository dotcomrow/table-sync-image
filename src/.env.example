# Table Sync Orchestrator Environment Variables# Table Sync Configuration

DATABASE_URL=postgresql://yugabyte:yugabyte@localhost:5433/yugabyte

# Scanning ConfigurationBIGQUERY_PROJECT_ID=your-gcp-project-id

SCAN_INTERVAL_SECONDS=30GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# YugabyteDB ConfigurationDEBEZIUM_CONNECTOR_URL=http://localhost:8083

YUGABYTEDB_HOST=yb-tserver-service.yugabyte.svc.cluster.localSCAN_INTERVAL_SECONDS=30

YUGABYTEDB_PORT=5433LOG_LEVEL=INFO

YUGABYTEDB_USER=yugabyte

YUGABYTEDB_PASSWORD=yugabyte# Debezium Configuration

DEBEZIUM_DATABASE_HOSTNAME=yugabyte-host

# BigQuery Configuration (project ID auto-derived from service account)DEBEZIUM_DATABASE_PORT=5433

GOOGLE_APPLICATION_CREDENTIALS=/vault/secrets/gcp-key.jsonDEBEZIUM_DATABASE_USER=yugabyte

BIGQUERY_LOCATION=USDEBEZIUM_DATABASE_PASSWORD=yugabyte

DEBEZIUM_DATABASE_NAME=yugabyte

# Kafka Connect Configuration

KAFKA_CONNECT_URL=http://kafka-connect.kafka.svc.internal.lan:8083# BigQuery Configuration

BIGQUERY_LOCATION=US

# Monitoring ConfigurationBIGQUERY_DATASET_LOCATION=US

LOG_LEVEL=INFO

HEALTH_CHECK_PORT=8080# Data Transfer Configuration

METRICS_PORT=8000TEMP_STORAGE_BUCKET=your-temp-storage-bucket

MAX_PARALLEL_TRANSFERS=3

# Processing ConfigurationBATCH_SIZE=10000
BATCH_SIZE=1000
MAX_RETRIES=3
RETRY_DELAY_SECONDS=5

# Configuration File
CONFIG_PATH=/app/config/orchestrator.yaml